# =============================================================================
# Data Cleaning OpenEnv — Python dependencies
# =============================================================================
#
# Install everything:
#   pip install -r requirements.txt
#
# Environment server only (no training):
#   pip install -r requirements/server.txt
#
# Training only:
#   pip install -r requirements/train.txt
# =============================================================================

#Server
fastapi>=0.111.0,<0.200.0
uvicorn[standard]>=0.29.0,<0.40.0
pydantic>=2.7.0,<3.0.0
openenv-core[core]>=0.2.1

#Core data
pandas>=2.2.0,<4.0.0
numpy>=1.26.0,<3.0.0
tabulate>=0.9.0

#Client
httpx>=0.27.0,<0.30.0

#Baseline inference
openai>=1.30.0,<2.0.0

#GRPO training
torch>=2.2.0
transformers>=4.40.0
accelerate>=0.30.0
tqdm>=4.66.0